/*
A Literal is a Literal representation of a value in JS. It is a fixed value - not a vaiable - they are literally provided in the code.

A variable is not a literal, the information assigned to the variable is the literal
  ex: 
  (keyword)           (value/literal)
    let placedholder = 'test';
         (varaibale)
*/

// STRING LITERAL
let city = "Indianapolis";

// NUMERIC LITERAL
let num = 123;

// BOOLEAN LITERAL
let isOn = true;

// OBJECT LITERAL
let movie = {
    title: 'Elf'
    rating: 'PG'
    releases: 'November 7th 2003'
};

// ARRAY LITERAL
let daysOfWeek = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];

